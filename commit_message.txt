feat: collab 브랜치로부터 오브젝트 생성 및 무기 동기화 로직 통합

- `public/object.js` 업데이트: `NatureObject` 클래스를 `collab` 브랜치의 더 포괄적인 버전으로 교체했습니다. 이 업데이트는 다양한 환경 오브젝트(건물, 자연, 자동차)에 대한 향상된 3D 모델 로딩 기능과 개선된 충돌 바운딩 박스 처리(단일 및 다중 바운딩 박스 지원)를 도입합니다. `NPC` 클래스는 변경되지 않았습니다.
- `public/weapon_system.js` 업데이트:
    - `WEAPON_DATA` 로딩 방식을 `public/resources/data/weapon_data.json` 파일에서 비동기적으로 데이터를 가져오도록 수정했습니다. 이는 하드코딩된 값을 사용하는 대신 무기 데이터를 외부에서 관리할 수 있게 합니다.
    - `WeaponModel` 클래스를 `collab` 브랜치의 `Weapon` 클래스 로직으로 대체하여 3D 무기 모델 로딩을 더욱 견고하게 처리합니다.
- `server.js` 업데이트: `comp/weaponUtils.js`의 서버 측 무기 데이터 로딩 및 유틸리티 함수를 통합했습니다. 이를 통해 서버가 무기 데이터를 일관되게 접근하고 관리할 수 있습니다.
- 리소스 파일 확인: 새로운 로직에서 참조하는 필요한 3D 모델 및 텍스처 파일이 `public/resources`에 이미 존재함을 확인하여 추가적인 파일 복사는 필요하지 않았습니다.